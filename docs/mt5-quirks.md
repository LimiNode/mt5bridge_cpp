# MetaTrader 5 quirks and recovery references

This is a searchable registry, not a promise that every forum workaround is
universally correct. Each entry records the symptom, safe bridge behavior, and
the upstream reference to re-check when MT5 or the Python package changes.

| Tag | Symptom | Bridge rule | Reference |
| --- | --- | --- | --- |
| `history-warmup` | First history request returns no/partial data while the terminal synchronizes its local series | Retry bounded history reads; distinguish empty history from unavailable history; expose diagnostics | [CopyTicks](https://www.mql5.com/en/docs/series/copyticks), [MQL5 forum 393357/page69](https://www.mql5.com/en/forum/393357/page69) |
| `history-timeout` | Synchronization can time out or return a partial result, including MQL error 4403 | Treat non-empty transient pages as provisional: do not publish or advance the cursor until a clean inclusive replay; never infer completeness from `size > 0` | [MQL5 error codes](https://www.mql5.com/ru/docs/constants/errorswarnings/errorcodes), [CopyTicksRange](https://www.mql5.com/ru/docs/series/copyticksrange) |
| `ipc-transport` | Python API reports `RES_E_INTERNAL_FAIL_*` (`-10000` through `-10005`) or returns partial data with an IPC status | Preserve committed data, call `shutdown()`/`initialize()`, and retry only idempotent reads from the inclusive cursor; expose reconnect counters | [Python last_error](https://www.mql5.com/en/docs/python_metatrader5/mt5lasterror_py), [MQL5 forum Python integration](https://www.mql5.com/en/forum/393357) |
| `series-sync` | Native series APIs expose an explicit synchronization state | Model warm-up as a state machine; do not retry every empty result forever | [Bars / series synchronization](https://www.mql5.com/en/docs/series/bars) |
| `python-history` | Python `copy_*` calls return `None` on failures and NumPy arrays on success | Convert `None` to classified bridge errors; copy numeric fields from the array buffer | [Python Integration](https://www.mql5.com/en/docs/python_metatrader5), [copy_ticks_from](https://www.mql5.com/en/docs/python_metatrader5/mt5copyticksfrom_py) |
| `side-effect-timeout` | `order_send()` can time out after the server accepted the order | Never auto-retry without reconciliation/idempotency | [Python order_send](https://www.mql5.com/en/docs/python_metatrader5/mt5ordersend_py) |
| `tick-timestamp-ties` | Several ticks may share the same `time_msc` | Use an inclusive cursor and a full-payload boundary multiset; never use `time_msc + 1` pagination | [CopyTicksRange](https://www.mql5.com/ru/docs/series/copyticksrange) |
| `same-ms-order-instability` | MT5 may return equal-`time_msc` ticks in a different order between inclusive reads | Match consumed boundary payload multiplicities instead of skipping positional rows; regression-test rotated buckets | [MQL5 forum 396877/page42](https://www.mql5.com/en/forum/396877/page42), [CopyTicks unexpected results](https://www.mql5.com/en/forum/370322) |
| `copyticks-before-from` | `CopyTicks` may include rows older than the requested `from` boundary | Filter rows before the requested boundary and keep a regression for inclusive pagination | [MQL5 forum 413688/page27](https://www.mql5.com/en/forum/413688/page27) |
| `poll-loss` | `symbol_info_tick()` returns only the current tick, so polling can miss updates | Use continuous history catch-up with `copy_ticks_from()` for reliable streams | [Python Integration](https://www.mql5.com/en/docs/python_metatrader5) |
| `independent-stream-order` | INFO and TRADE streams may be inserted into history out of timestamp order; a later-observed tick can have an earlier `time_msc` | Re-read a bounded overlap and reconcile by full-payload multiplicity; never treat the latest timestamp as a permanent high-water mark | [MQL5 forum 396877/page19](https://www.mql5.com/en/forum/396877/page19), [MQL5 forum 42122/page20](https://www.mql5.com/ru/forum/42122/page20) |
| `copyticks-lag` | `CopyTicks`/history synchronization can lag behind the live terminal and may take hundreds of milliseconds or longer | Keep periodic polling even when a cheap head hint is unchanged; use adaptive overlap and expose history lag | [MQL5 forum 342090/page57](https://www.mql5.com/ru/forum/342090/page57) |
| `timeout-with-data` | A non-empty `CopyTicks` result can still carry `ERR_HISTORY_TIMEOUT`/synchronization status | Inspect result and `last_error` together; retain data for reconciliation but do not emit a stable READY/caught-up state until a later confirmation succeeds | [CopyTicks](https://www.mql5.com/en/docs/matrix/matrix_initialization/matrix_copyticks), [MQL5 forum 396877/page43](https://www.mql5.com/en/forum/396877/page43) |
| `history-rewrite` | A running or freshly restarted terminal can expose different history for the same interval | Promise only best-effort lossless delivery relative to observable synchronized history; detect and report rewrites instead of claiming broker-level guarantees | [MQL5 forum 396877/page46](https://www.mql5.com/en/forum/396877/page46) |
| `ontick-coalescing` | `OnTick` is a market-state notification: a new event is not queued while another NewTick is pending/processing | Do not use an MQL `OnTick` bridge as an exchange-tick transport; use CopyTicks history reconciliation | [OnTick](https://www.mql5.com/en/docs/event_handlers/ontick), [OnTickMulti discussion](https://www.mql5.com/ru/forum/458908) |
| `multisymbol-view-not-atomic` | A multisymbol `OnTick`/state view can expose different symbols at different processing points | Keep raw grouped subscriptions non-coherent; reject snapshot delivery until common watermark/staleness semantics exist | [OnTickMulti discussion](https://www.mql5.com/ru/forum/458908), [MQL5 forum 370322](https://www.mql5.com/en/forum/370322) |
| `access-point-switch` | Automatic access-point changes can stall history or return `4403` until synchronization recovers | Enter RECONNECTING, apply bounded exponential backoff, verify connectivity, then reconcile an enlarged overlap | [MQL5 forum 42122/page49](https://www.mql5.com/ru/forum/42122/page49), [CopyTicks](https://www.mql5.com/ru/docs/series/copyticks) |
| `copyticks-latency` | CopyTicks initiates server synchronization and can block for a long time on poor connectivity | Never hammer the API at a fixed 10–20 ms cadence during recovery; use retry deadlines/backoff and publish diagnostics | [CopyTicks](https://www.mql5.com/ru/docs/series/copyticks) |
| `trade-reconciliation` | After a trading call, orders/deals/positions may become mutually consistent only after a delay; a timeout may arrive after server acceptance | Persist `dispatch_intent_persisted`, verify the immutable AccountKey and single-writer lease, then commit durable `dispatching` before the one `order_send`; reconcile by ticket/identifier graph and never resend after that barrier. Report `CONFIRMED`/`PENDING`/`NOT_OBSERVED`/`AMBIGUOUS` outcomes; `NOT_OBSERVED` never proves absence, does not end the operation lifetime, and never permits retry | [MT4Orders discussion](https://www.mql5.com/zh/forum/164943/page64), [order_send](https://www.mql5.com/en/docs/python_metatrader5/mt5ordersend_py) |
| `trade-request-id-scope` | `request_id` correlates the native request transaction but not every later order/deal/position transaction; Python does not expose `OnTradeTransaction` | Treat `request_id` as optional evidence in one operation, never as the trade identity; use the reconciliation graph | [OnTradeTransaction](https://www.mql5.com/en/docs/event_handlers/ontradetransaction), [TradeTransactions](https://www.mql5.com/en/code/22166) |
| `trade-result-tickets` | A successful market result may have `deal == 0` or only an order ticket, especially with external execution | Preserve `retcode`, `retcode_external`, `order`, and `deal`; reconcile later instead of promoting one field to position identity | [MqlTradeResult](https://www.mql5.com/en/docs/constants/structures/mqltraderesult), [MQL5 forum 271445](https://www.mql5.com/en/forum/271445) |
| `trade-history-ordering` | Deals and history orders can become visible in different phases; history order arrays are not chronological append-only queues | Use ticket-keyed snapshots and bounded overlap reconciliation; never use the last element as “the new order” | [MQL5 forum 357245](https://www.mql5.com/en/forum/357245), [MQL5 forum 454011](https://www.mql5.com/en/forum/454011), [MQL5 forum 97505/page11](https://www.mql5.com/en/forum/97505/page11) |
| `position-identity` | `POSITION_TICKET` and `POSITION_IDENTIFIER` have different semantics; pending/partial execution can change their relationships | Keep both fields separately and link history by `POSITION_IDENTIFIER`; never equate either with the bridge `TradeId` | [HistorySelectByPosition](https://www.mql5.com/en/docs/trading/historyselectbyposition), [MQL5 forum 77142/page6](https://www.mql5.com/en/forum/77142/page6) |
| `trade-partial-fill` | One request may produce multiple deals and a pending remainder; cancel may preserve an earlier fill | Model one logical operation with a deal set and explicit remaining volume, not one final ticket | [MQL5 trade transactions](https://www.mql5.com/en/docs/event_handlers/ontradetransaction), [MQL5 forum 357245](https://www.mql5.com/en/forum/357245) |
| `trade-account-mode` | Hedging keeps separate positions while netting combines logical trades into one position; manual/SL/TP changes can interfere | Maintain a logical attribution ledger; report `AMBIGUOUS` after an external mutation instead of inventing ownership | [MQL5 position properties](https://www.mql5.com/en/docs/constants/tradingconstants/positionproperties), [HistorySelectByPosition](https://www.mql5.com/en/docs/trading/historyselectbyposition) |
| `trade-hedge-permission` | `ACCOUNT_MARGIN_MODE_RETAIL_HEDGING` describes position accounting, not whether opposite positions are permitted; the Python `account_info()` record may omit `ACCOUNT_HEDGE_ALLOWED` | Keep `hedge_allowed` unknown unless the backend reports the dedicated field; use the account `known_fields` mask and never infer permission from `margin_mode` | [Account properties](https://www.mql5.com/en/docs/constants/environment_state/accountinformation), [Python account_info](https://www.mql5.com/en/docs/python_metatrader5/mt5accountinfo_py) |
| `trade-execution-mode` | `SYMBOL_TRADE_MODE` (direction permission) and `SYMBOL_TRADE_EXEMODE` (request/instant/market/exchange execution) are different values; filling rules depend on execution mode | Expose both fields and require `trade_exemode` to be known before resolving FOK/IOC/RETURN/BOC; do not substitute `trade_mode` | [Symbol properties](https://www.mql5.com/en/docs/constants/environment_state/marketinfoconstants), [Python symbol_info](https://www.mql5.com/en/docs/python_metatrader5/mt5symbolinfo_py) |
| `order-check-result-shape` | `MqlTradeCheckResult` has no `retcode_external`; that field belongs to `MqlTradeResult` returned by `order_send` | Keep typed `order_check` limited to the documented check-result fields; preserve `retcode_external` only in the future durable send/journal result | [MqlTradeCheckResult](https://www.mql5.com/en/docs/constants/structures/mqltradecheckresult), [MqlTradeResult](https://www.mql5.com/en/docs/constants/structures/mqltraderesult) |
| `trade-filling-mode` | FOK/IOC/RETURN support depends on symbol execution and filling capabilities; RETURN is forbidden for some market execution modes | Inspect symbol capabilities, resolve a compatible policy, run `order_check`, and reject invalid requests before `order_send` | [Symbol properties](https://www.mql5.com/en/docs/constants/environment_state/marketinfoconstants) |
| `trade-closeby-capability` | `CloseBy` is available only when the symbol permits `SYMBOL_ORDER_CLOSEBY`, normally on hedging accounts | Expose the capability explicitly and preserve both `ORDER_POSITION_BY_ID` and `DEAL_ENTRY_OUT_BY` links | [Symbol properties](https://www.mql5.com/en/docs/constants/environment_state/marketinfoconstants), [Close By](https://www.mql5.com/en/docs/trading/closeby) |
| `trade-account-switch` | The terminal account can change between baseline, send, and reconciliation | Bind an immutable `(server, login)` AccountKey; require matching account capabilities and a single-writer lease before durable `dispatching`; block a side effect on mismatch and suspend reconciliation with `ACCOUNT_MISMATCH`; scope graph keys by AccountKey | [Account properties](https://www.mql5.com/en/docs/constants/environment_state/accountinformation) |
| `trade-reconciliation-lifetime` | History/deal visibility can lag beyond an initial wait deadline | Emit `NOT_OBSERVED` only as a time-bounded observation, keep the journal operation unresolved, and allow later snapshots/reconnect/restart to reconcile it; never retry automatically | [MQL5 forum trade synchronization](https://www.mql5.com/en/forum/393733/page1723) |
| `trade-partial-retcode` | `DONE_PARTIAL` has different terminality under IOC versus RETURN | Classify with operation kind, filling/execution mode, and observed remainder: IOC may cancel the remainder, RETURN may leave it active | [Trade return codes](https://www.mql5.com/en/docs/constants/errorswarnings/enum_trade_return_codes), [Symbol properties](https://www.mql5.com/en/docs/constants/environment_state/marketinfoconstants) |
| `trade-event-overflow` | `OnTradeTransaction` delivery is out of order and the terminal queue holds only 1024 events | Treat transactions as hints, copy quickly to a bounded ring, emit GAP/overflow, and trigger full snapshot reconciliation | [OnTradeTransaction](https://www.mql5.com/en/docs/event_handlers/ontradetransaction) |
| `python-no-native-async` | The Python package exposes synchronous `order_send` but not terminal `OrderSendAsync` or transaction callbacks | Implement caller-asynchronous behavior with a dedicated worker only; call it Python-backed async, not terminal-native async | [Python Integration](https://www.mql5.com/en/docs/python_metatrader5), [OrderSendAsync](https://www.mql5.com/en/docs/trading/ordersendasync) |
| `cpython-restart` | Binary extension modules may retain process-global state across `Py_FinalizeEx()` | Do not promise restart-in-process until a 100-cycle packaged-runtime/live-terminal acceptance test passes; prefer a worker process if unstable | [CPython initialization/finalization](https://docs.python.org/3/c-api/init.html) |

## Realtime interpretation rule

Realtime delivery is **best-effort lossless relative to the observable,
synchronized MT5 history**. It is not a broker-level guarantee: MT5 may insert,
reorder, delay, or rewrite history after a tick was observed. The source must
therefore poll an overlap window, reconcile complete tick payloads with
multiplicity, keep explicit partial/reconnecting diagnostics, and expose
consumer overflow separately from source/history inconsistency.

## Search playbook

Use these terms when investigating a new symptom:

```text
site:mql5.com/en/forum MetaTrader5 copy_ticks first request empty
site:mql5.com/en/forum MetaTrader5 history synchronization timeout Python
site:mql5.com/en/forum MetaTrader5 copy_rates None
site:mql5.com/en/docs/python_metatrader5 copy_ticks_from last_error
```

When a workaround is confirmed, add a row with the observed terminal build,
Python package version, reproduction, bounded recovery rule, and link. Keep the
implementation in the bridge reliability layer rather than copying a version
check into every consumer.
