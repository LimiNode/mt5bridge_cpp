/// \file header_self_containment_trade_positions.c
/// \brief Compiles the trade positions C header as a standalone translation unit.

#include <mt5bridge/trade/positions.h>

int mt5bridge_probe_trade_positions_header(void) { return 0; }
